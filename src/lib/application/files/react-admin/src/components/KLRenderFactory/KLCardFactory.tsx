import type { FC } from 'react'

interface KLCardFactoryProps {
  items: Record<string, any> | Record<string, any>[]
}

const KLCardFactory: FC<KLCardFactoryProps> = ({ items }) => {
  return <div>TODO</div>
}

export default KLCardFactory
