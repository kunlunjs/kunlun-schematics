import type { FC } from 'react'

interface KLChartFactoryProps {
  items: Record<string, any> | Record<string, any>[]
}

const KLChartFactory: FC<KLChartFactoryProps> = ({ items }) => {
  return <div>TODO</div>
}

export default KLChartFactory
